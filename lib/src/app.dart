import 'package:flutter/material.dart';
import 'package:taxi_app/src/screens/login_page.dart';

class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return(
        MaterialApp(
            home: LoginPage()
        )
    );
  }
}